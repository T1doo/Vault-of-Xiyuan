# 文档仓库读取入口

这是短入口，不复制本机私人规则，不扩大任务授权。

- 开始时检查实际 cwd、Git 状态与适用规则，保留未提交工作。在本项目服务器显式读取 `../AGENTS.md`（相对于本仓库根；布局不同先定位工作区根），不假定 Git 根外规则自动加载。
- 从本仓库 README.md 进入当前阶段，读取其 PLAN.md、LOG.md 顶部及最近记录；按需查阅 docs/CODEX_EXECUTION_GUIDE.md、docs/EXPERIMENT_PLAN.md，首次进入项目须理解两份主文档。
- 每阶段只维护 PLAN/LOG：具体步骤及完成条件在 PLAN，进展、决定、实际命令、证据与阶段总结在 LOG。结束只更新当前LOG，步骤实证完成才勾选PLAN；切换阶段才更新README入口。
- 不再另建任务、会话、gate或全局账本目录/文件。保留日志历史，原始实验产物留工程/运行目录。
- 外部克隆/worktree 路径不同先定位权威来源；缺私人规则时明确边界，纯文档审阅可继续，本机实验先补齐规则。新会话加载验证按手册2.4执行，入口存在不代表通过。
- 负责人持续授权：每次完成工作后，检查并提交、推送本仓库的本次相关改动，最终回复附提交标识及对应文件的GitHub固定版本链接；无新改动则确认同步。保留无关用户修改，不强推、不公开私人规则/凭据/数据/权重。若用户另行要求暂不发布则遵守；推送失败须明确说明，不能只提交本地却声称已发布。
