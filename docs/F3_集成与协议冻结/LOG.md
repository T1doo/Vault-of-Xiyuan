# F3：集成与协议冻结——阶段日志

## 当前进展

最后核对：2026-09-15。**F3=TODO / PLANNED，现有PLAN已细化，等待审阅后执行；无本阶段模型/仿真作业或checkpoint。** F2限定性DONE获负责人最终认可，G1保持PASS，G2未通过。

本轮只编辑阶段PLAN/LOG、README入口及F2聚合JSON的历史字段作用范围。未启动训练、推理、SAB、教师提取、评测或性能测量；既有F2模型/源码/曲线/失败记录均保留。

下一步：审阅PLAN末尾两项提案（四组各400步的有限全清单开发与24 GPU小时上限）；未获计划执行批准前不启动计算。具体正式K/队列/协议在后续G2另行冻结，不能因F2完成而自动批准。

## 执行记录

尚无本阶段实际执行记录。

### 2026-09-15｜承接F2最终评审，细化F3计划（仅文档）

负责人最终认可文档提交`86bb25eb13867a40ca557f232b1d3b633bcd3d86`对应F2限定收尾：A/B独立工程、监督、小训练与规定评测已完成，稳定控制目标未达成；不再重训同一500池、重做已通过回归/QC/缓存或重复20回合追成功。有效结果固定SA full-A原0/20（26请求/6合法/30执行步）及SB修复v1原20单元重评0/20（31请求/11合法/55执行步），旧action-only缺陷结果只作历史。

本轮核对文档工作树并读取当前F3简要计划/迁移日志、README，参照工程手册T07—T09、§8.5以及主计划有关公平性、梯度、统计、G2/预算条款。保留旧迁移记录，不将“当前仍F0”的占位内容继续当现状。

现有PLAN细化为公共训练/推理入口、仅新增集成路径检查、全train清单下有限开发、真实评测/统计冻结、实测预算/恢复和G2条件。首批提案为四组各400步（首100步单列，后300步profile）、有效batch16、容量144、候选K20k的原公共日程；不压缩warmup、不将这批短测当充分学习。首批结束不自动延长，系统性自由生成失败必须解释或形成明确后续预算决定，不能直接扩成正式长训练。24 GPU小时、最多4空卡为待审提案，不是已运行成本或队列许可；超预算保留完整恢复点，不更改batch或悄悄删组。

评测计划复用实际开发库存、保留布局资源不足事实，明确dev/final/底层初态关系、指令透传、请求/回合分母、raw token及版本、基础设施有限重试与策略失败不重试、配对簇统计及同K终点。F2的GPT技术QC采用不重新设为未批准，human_reviewed=0及独立备份未知限制继续保留。F3只维护本PLAN/LOG，不新增管理目录或调度平台。

F2聚合JSON仅作用域整理：原顶层`service_replay`和`new_gpu_seconds=0`移入`history.f2_final_check_before_service_fix`，关联CPU重放批次及`ae6f908…`；新增引用字段指向`service_fix.regression`和`service_fix.cost`作为当前服务修复证据。数值和原意均保留，不重算实验，也不覆盖旧服务/失败原件。此次跨阶段衔接决定记于F3 LOG，不再打开F2实验工作。

本次交付为可执行/可验收的计划与两项具体待审决定；所有执行步骤仍未勾选。没有新的模型/环境验证结果，G2不通过、不启动任何正式作业。提交/推送结果以本轮最终固定SHA为准。

文档验证退出0：`git diff --check`、F3计划无执行完成勾选、Markdown链接/围栏、聚合历史字段搬移前后等值及其余实验字段完全不变。只修改F3 PLAN/LOG、README与上述聚合JSON；未运行模型或仿真测试。
