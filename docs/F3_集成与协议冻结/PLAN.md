# F3：集成与协议冻结——阶段计划

## 当前状态、目标与授权

2026-09-15最新决定：**F3=TODO / PLANNED，F2前置未满足，暂停执行。** 四组各400步、24 GPU小时、最多四卡及配套评测/自动续接的执行授权已撤回；以下内容保留为未授权提案，不得据旧条目启动。需先完成F2功能验收并形成新的明确技术结论，之后再审阅F3。当前仅允许F2状态更正、CPU分析和提案写作。

目标：完成T07四组真实入口集成及有限开发验证，收齐T08评测清单、T09启动/恢复与统计合同，在G2形成公平、可解释且预算明确的正式方案。F3计划通过不等于G2通过，G2与正式队列授权另行登记。

依据：[工程手册](../CODEX_EXECUTION_GUIDE.md) §8.5、§9、T07—T09和[主计划](../EXPERIMENT_PLAN.md) §7.6、§8—12。执行记录只写本阶段LOG；配置、manifest、协议锁和原始回执留既有工程目录，不新建管理文件或通用调度平台。

## 继承F2的事实与限制

F2此前限定DONE作为整体验收和F3前置通过的效力已由2026-09-15最新决定撤回；工程实现、监督/缓存、500池训练、恢复/性能及评测执行事实保留。F2=IN_PROGRESS，稳定控制目标未达成；G1=PASS，不重验F1。固定依据为[服务修复及收尾记录](../F2_AB模块实现/LOG.md)和文档提交`86bb25eb13867a40ca557f232b1d3b633bcd3d86`。

| 继承项 | F3使用方式 |
|---|---|
| data-v2：450/50轨迹、55,682/6,068动作起点 | 不重划分；保持obs[i]↔states[i+1]↔actions[i+1:]、train-only norm、确定性图像和严格FAST |
| A采用标签、GPT技术复核300题、human_reviewed=0 | 承认已批准采用，不重新请求同一QC许可；接触代理、2 mm、阶段unknown/少数类覆盖限制如实带入。将来确需真人证据的独立要求不因此解除 |
| 完整教师缓存及质量采用 | 保持FastVGGT合同、两个真实视图、第12层学生输出与均匀对齐；复用完整性证据，无变化不重读全缓存 |
| SA full-A与修复版SB有效结果 | SA 0/20、26请求/6合法/30执行步；SB 0/20、31请求/11合法/55执行步。两份旧action-only缺陷结果只作历史 |
| 类型适配及真实服务回归 | 复用经验证generated_token_ids、严格子错误及raw token记录；同一F1模型/144容量两输入回归已通过，不再复制评测服务 |
| 两组原3,000步checkpoint | 开发诊断/排错参照，不是正式初始化；不对同一500池追加训练 |
| 训练内/val诊断 | 10个train样本确有曝光；SA两次失败对应action-only训练而full-A推理，条件不同。小集合差异不能解释为唯一泛化原因 |
| 编译缓存与成本 | 保留已验收的私有缓存；五步26.184/28.589秒是短基准。56,781/62,387秒只覆盖恢复后1001—3000，旧暂停成本未知；独立备份未确认 |

## 1. 先统一真正使用的实现（T07）

- [ ] 核对实际代码commit/dirty差异和训练、修复服务、A生成helper的源码hash；保留原件，确定一个集成者维护公共入口。列出受影响调用链及复用证据，不凭LOG声称代码已合并。
- [ ] 在既有训练入口与正式服务路径内实现s/sa/sb/sab配置分派。数据准备、优化/累计、检查点及公共动作生成/适配只保留共同实现；诊断和评测客户端调用相同服务，不建立临时替代推理器。任何新增CLI先实现、--help和输入校验后登记可执行命令，本计划不给假定已存在的CLI。

| 组 | 训练监督/损失 | 推理依赖 |
|---|---|---|
| S | action | 公共RGB/state/当前原始指令 |
| SA | action + λA direction | 模型生成完整方向段后接动作；不读GT |
| SB | action + λB alignment | action-only；不装teacher/cache/projector/A标签 |
| SAB | 三项损失 | 与SA相同方向→动作；B依赖训练结束后全部移除 |

- [ ] A：承接共用问题前缀、两角色顺序、完整候选序列、同一KV续写；方向/问题错误才重新建立原始S前缀，动作错误直接失败。训练invalid样本整样本S action-only，保留动作与采样位置；推理不得查GT valid。仅λA=0不作为结构关闭等价测试。
- [ ] B：单次学生前向同时提取动作和第12层输出，仅真实512视觉位置均匀对齐；attention mask与alignment mask分离。投影头只训练；S/SA不读教师、S/SB不读方向标签。读取隔离检查覆盖真实服务/loader和辅助脚本，不能以未使用某变量代替未读文件。
- [ ] 所有实际服务复用经验证token适配，有限整数浮点保持ID不变；子错误、原始token、checkpoint/服务版本与回合/请求键一起落盘。SA原helper若迁入公共模块，先对保存输出和相同生成边界做针对性回归；未受影响的旧证据直接引用。

完成证据：四组配置/依赖差异可审阅，共用入口实际可调用；无监督推理对象、参数白名单、输入校验与raw记录均从实际服务验证。

## 2. 集成新增路径的检查，不重复未变化测试

- [ ] 四组从同一pi0_fast_base、同seed共享LoRA逐叶相同，B投影头独立RNG不扰动共享初值；冻结SigLIP/原Gemma，optimizer白名单为共享LoRA及B投影头。
- [ ] 对新增SAB组合检查：A/B结构关闭恢复S、B关闭无读取/计算、λB=0公共动作路径一致；真实后缀替换不改变视觉prefix；q=0仅改目标特征不影响alignment；真实视图span、单次前向、学生上游梯度与冻结叶子行为正确。
- [ ] 三项损失各按每样本有效位置归一化，再batch平均；无方向样本方向项为0但动作有效token>0。梯度按样本数累计后统一裁剪，共享LoRA每有效步更新一次，scheduler/optimizer共用该计数。
- [ ] 改动后的保存/恢复验证真正模型、LoRA、B投影头及optimizer/RNG/sampler/cursor/code与监督身份；用首批运行保存点的新进程恢复和对应下一步参考核对，不另开无穷多轮三步训练。分支诊断更新单独计数，不混入正式预算。
- [ ] 模型输入内确定性预处理与缓存源图对应；任一缺缓存、错误ID/hash/维度、不可信恢复状态、非有限值或后缀泄漏立即停止受影响路径。已有类型回归与原S KV解释不重跑。

判定：ID/token/mask/shape精确一致；浮点沿用对应固定配置的既有判据，若新配置无依据，先做有界参考重复取得误差范围再比较，不遇错放宽。学习好坏与实现正确性分别记录。

## 3. 完整数据清单下的首批有限开发验证（待批准）

- [ ] 训练池切到完整55,682动作清单，四组同排序manifest、seed0及采样算法，保留重复样本顺序；各自从基础权重起训。A连接已批准完整train标签，SB只接train缓存，val不更新参数。配置/服务版本与F2开发模型明确区分。
- [ ] **提案：四组各400有效更新**，microbatch4×累计4、有效batch16、容量144、原BF16及确定性设置。首100步单列加载/编译/预热，后300步统计完整更新成本，满足手册T07的200—500步smoke及至少300步profile要求；单独少量trace不计入稳态速度。保存100/400及必要异常恢复点。该规模待本计划审阅，不是本轮启动授权。
- [ ] 沿用AdamW β0.9/0.95、eps1e-8、weight_decay0.01、统一裁剪1、LR warmup1000、3e-5→3e-6、λA=0.3、λB前2000步递增到0.1。首批建议以候选K=20,000的公共学习率日程运行，只在400步截停；不为了短测把warmup压缩成效果实验。若选择不同候选K，在开跑前统一resolved config，不能继承F2的3000步衰减末端或启动后改日程。

400×16=6,400次曝光，仅约全train动作数的11.5%抽取量，实际唯一曝光更少；“完整清单采样”不是“遍历全部数据”，不能用这批证明充分学习或更多数据一定解决失败。

- [ ] 采样计划生成后、观察预测之前，固定每任务一条实际已曝光观测及每任务一条独立val观测（共10+10，四组共用）；另锁定开发环境单元。报告输入来源/曝光次数、实际训练打包、方向有效性、teacher-forcing与自然生成/严格动作合法性，不能把SA action-only训练和full-A推理当作相同条件。
- [ ] 含A模型只从公开输入自回归预测，离线用可靠GT评分完整方向词和pair、混淆矩阵/类别召回及覆盖；沿用train确定的多数类和并列规则，不用val选类。基于已有F2模型做少量预测/GT/错误方向离线诊断时，单列为diagnostics，不加训练模型或把GT开放到在线策略。
- [ ] 复用F1已有多checkpoint S趋势，补首批四组动作/辅助损失、自由生成错误分布及共享LoRA梯度尺度。固定小批分别计算动作、λA方向、λB对齐梯度范数与比值；不含投影头，不设同向/最佳比例门槛；动作梯度近零注明不稳定，不自动调λ。
- [ ] 初次闭环使用已有20个clean开发单元，四组同单元/seed/控制；每组仅第400步一次，保留所有结果。七维各一个生效/透传单例与执行器检查另行登记；手册3任务×5×clean/视角/布局=45回合/模型为可选扩大开发包，不默认为首批额度，须先落实库存隔离与预算。

首批判读与停止：程序/数据/恢复错误先修复受影响部分；已见与val均自然生成失败时继续区分接口/序列学习，不直接写环境泛化差；已见较好而val/环境差只支持有限覆盖/分布差异假设。所有组仅400步且尚在warmup，不以低成功率否定方法，也不据“smoke完成”认为可以正式放大。

首批结束即交付一次报告，不自动延至3000或20k。若正式比较仍被系统性格式失败主导、无法形成有解释力的控制实验，G2保留未决：报告数据/学习进度证据和具体下一步，负责人决定一次额外的统一有限全清单开发预算或暂缓正式实验；不得反复给F2的500池加训、改变标签/教师或自行追加研究支路。

## 4. 基于已有真实库存冻结评测与统计（T08）

- [ ] 读取F1/F2库存、已用开发unit_fingerprint及底层状态关联，整理真实dev/core/final/可行扩展manifest。只补缺失的加载/reset/生效证据，不再盘同一批资产。布局每配置一个初态和任务不足20同类配置的限制已接受，使用真实数量或已允许的多个同维配置，不复制状态/换无作用seed凑数。
- [ ] 逐项冻结eval_id、官方配置/变体、指令原文、初态与来源、底层状态簇或unknown、随机因素、执行规则、超时/成功和包内权重。开发实际单元不进入final调参，未知关联保守处理，不凭episode split声称严格初态留出。若某维无可用隔离单元，列具体资源缺口，不挪最终唯一单元调参。
- [ ] 动作请求仅公共两路RGB/本体状态/原始指令；七维扰动真实传入。四组在同eval_id共享对应初始条件，机器人姿态扰动不能为配对改回clean。环境/episode/服务并发隔离，记录renderer与GPU资源。
- [ ] 结果同时保留回合与请求：服务/基础设施错误、方向解析/生成回退、FAST dtype/ID/Action/EOS/长度/非有限错误、无效物理动作、超时、成功；子错误与raw token绑定版本和请求编号。缺历史字段不补造。策略失败不重试，基础设施按同ID/seed最多额外两次，attempts保留，主表不挑成功尝试。
- [ ] 正式默认P1-Lite、seeds0/1/2；先seed0四组P0，具体任务数/回合数由真实清单计算，不复用理想19,200作事实。每组同第K步，不看最终测试挑checkpoint；诊断/F2结果不混正式表。
- [ ] 冻结配置内平均→包内等权/预定权重→任务/七维macro的聚合；clean单列，缺失/共同覆盖单列。按可验证底层初态簇配对bootstrap，同簇跨组/跨条件共同抽样，训练seed分别展示。单状态不虚构方差，unknown不当独立；统计fixture验证空配置/重复attempt和权重。报告R、条件增益、I_AB及G_drop并保留适用范围，不用bootstrap符号比例伪造p值。
- [ ] 以真实清单做有限精度/资源情景分析，不从一个pilot seed推断训练方差或承诺显著收益；扩展包依预登记资源条件启用，不按效果选择。

## 5. 实测预算、恢复与正式启动边界（T09）

- [ ] 沿用F2私有编译缓存及已验收执行语义；公共入口迁移需绑定code/config/data/cache/protocol hash，只在有数值与恢复证据后采用，不自动合入被拒的大JIT候选。
- [ ] 首批拟申请**24 GPU·小时总上限**，包括四组400更新、受影响回归/恢复、profile及首批开发评测，最多4张确认空闲卡、一卡一个主要作业（renderer同卡或单列计入），不改microbatch或有效batch。这是可审阅预算提案，不是实测成本或已授权队列；空卡授权不替代本轮计划审批。
- [ ] 起步用F2两组五步26.184/28.589秒及恢复段均值约28.39/31.19秒作参考，四组S/SAB和全数据读取成本未知。以30—45秒/有效更新假设，四组400步纯更新约13.3—20 GPU小时；尚有加载/评测，24小时不保证全部任务完成。首次真实100—400窗口测得后更新余量，不能用历史6秒/步或短基准代替全数据吞吐。
- [ ] 每条已完成更新记录同步完成时间、读取/预热/保存标签、实际lr/λ、有效计数、裁剪、异常与采样cursor。保存完成/可恢复点和heartbeat分开，异步保存确认完成后才登记。私有编译缓存不是checkpoint，不能作为唯一恢复依赖。
- [ ] 接近预算（保守预计下一完整更新+保存会超额度）不再发新更新；保存当前完整状态并停止，报告哪些组/阶段未完成，不静默删组/减batch/换样本。启动前预留保存/退出余量，慢于假设时先缩短未启动作业队列，不冒险运行至预算后才处理。预算内正常无需每步再问许可；新语义冲突或正确性异常停止受影响任务。
- [ ] 复用最小runner/唯一写入锁，启动核对空卡/进程/产物，拒绝重复run或覆盖；测试fixture可在G2前完成，不以“依赖G2”制造循环。无人值守只能执行已经验证且批准的固定队列，有总额度/超时/有限重试/停止条件，不建通用调度平台。
- [ ] G2前以每组实际步时t、正式K和真实N_eval估算训练Σ(seed×K×t)、评测/加载/备份/排错及缓冲。K=20,000只是候选，32万曝光不等于独立样本；保存规则及学习率衰减随K统一冻结。历史预计节省41.46 GPU小时不作实结；暂停占卡和旧缺失成本单列。独立备份未确认保持风险披露，不向外部云自动上传。

## G2完成条件与阶段交接

- [ ] 四组真正共用入口、SAB组合新增测试、参数/数据/因果性/无监督服务/累计与恢复均有原件证据，未变化路径明确复用来源。
- [ ] 有限全清单开发与原S曲线、方向/辅助梯度/错误分析能解释候选预算和正式比较目的；系统性失败若仍影响可解释性，给出具体未决项，不只写待签字，不自动放大。
- [ ] 真实dev/final/core/扩展清单、初态关联与统计规则、异常/attempt/raw记录、终点K/初始化/lr/λ及成本范围可审阅，已批准QC及human_reviewed=0限制明确。
- [ ] `resolved configs`与`protocol.lock.yaml`在既有工程目录固定，源码/依赖/数据/norm/tokenizer/标签/缓存/清单hash齐全；命令确已实现和验证。
- [ ] 负责人确认G2协议和具体正式队列（组/seed/卡/时限/总预算/评测/恢复/停止条件）后才进入F4；本计划审阅不授权正式十二作业。F3不要求增强组胜S，也不把负结果变成效果PASS。

## 本轮需要审阅决定的两项

1. **首批开发规模与日程**：是否采用上述四组各400步、完整train清单采样、候选K=20k公共日程（不压缩warmup），终点20个clean开发单元与七维单例；400步之后先报告，不自动延长。它用于实现/可解释性检查，不保证控制学习充分。
2. **首批资源上限**：是否采用24 GPU小时、最多4张确认空闲卡的批次上限；若实测不足则保留完整恢复点并交付已完成范围。正式K、三seed总预算/清单及无人值守授权留G2，不在本轮提前确认。

当前以上均PLANNED。本次提交后等待计划审阅，不运行任何一项模型或仿真计算。
