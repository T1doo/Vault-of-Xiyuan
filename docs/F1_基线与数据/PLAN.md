# F1：基线与数据——阶段计划

## 目标

完成 T01/T02，前置 T08 库存，获得可信且可学习的项目 S。

进入条件：F0 盘点及实际执行所需规则/路径已明确。

共用约束见 [实验计划](../EXPERIMENT_PLAN.md) 与 [工程手册](../CODEX_EXECUTION_GUIDE.md)。以下记录计划和实际完成范围；已勾选项目均以日志证据为准，未勾选项目属于后续阶段。

> **收尾状态（2026-09-08）：F1 已完成，G1=PASS，等待下一阶段指令。** 本次技术审阅由负责人委托 GPT 完成，审阅结论通过基础链路和 S 可学习性，同时保留已知限制与后续技术事项。

## 已完成的首批执行范围（2026-09-07）

负责人根据F0复审批准进入环境准备。先完成固定uv/Python3.11和policy-train依赖安装，以及CPU导入/CLI与资源允许时的最小GPU编译计算；不下载模型权重或数据，不转换/回放/训练。其余环境尚不视为完成。

- [x] 固定uv与Python3.11，保留来源、版本和校验（uv0.12.10 / CPython3.11.16，证据见LOG）。
- [x] 根据固定openpi锁文件安装隔离policy-train，核验依赖与源码导入位置（202包检查及锁一致性通过）。
- [x] 检查实际CLI和最小计算，分别记录安装、导入、GPU编译的结果与限制（train/serve help、CPU导入、单卡合成JIT/梯度/BF16通过；不是模型测试）。

## 当前授权

负责人已设置持续目标“完成F1”。继续全部实施步骤与G1要求；首批仅学生环境的范围是历史批次，不再限制后续数据、模型、仿真与开发训练。正式四组长训仍须G2，人工验收不得代签。

**GPU资源授权更新（2026-09-07）：只要有空闲GPU就可以用。** 不再以两张为上限，可增加资源预算，将全数据训练、开发评测、累计/确定性验证等无依赖工作分配到所有已确认空闲卡并行，无需因超过两卡再次询问。GPU统一登记，不改动正在运行的训练配置或有效batch。

## 实施步骤

- [x] 固定学生、教师、clean/plus 来源版本，建立四个隔离环境与独立 LIBERO 配置；依赖/必要导入与已使用CLI已验证，教师实际模型执行留F2（证据见LOG）。
- [x] 下载并核验 pi0_fast_base、FAST tokenizer、Spatial 原始演示及七维所需官方资源（固定revision/hash与七维单例记录见LOG）。
- [x] 联合盘点训练—开发—最终底层初态、官方配置、唯一 reset 和生效随机性；已在 norm/标注/缓存前决定不重划训练集，并保留初态跨来源血缘未知、布局同维度库存不足等限制。
- [x] 验证两视图、状态/动作语义与时间同步；10 个任务均完成专家回放（9/10 成功，失败保留），完成动作正逆变换、FAST 回环和 100 个真实样本检查。
- [x] 实现项目 S 共用严格解码/溢出检查、冻结白名单和模型入口确定性预处理；真实累计训练与实际样本的GPU证据已通过，生成长度稳定性归后续学习/闭环验收。
- [x] 按完整episode建立固定划分、稳定sample_id、train-only norm和可恢复采样；300步实际采样逐步复核通过，作为四组共用基础。
- [x] 按主文档执行小样本与 1k—3k 开发训练、保存恢复和多检查点闭环；小样本恢复、2,000 步多检查点闭环、独立 3,000 步延长及固定 20 单元终点评测均已记录，未使用最终单元调参。

## 完成条件

G1：数据—动作—仿真链路正确、S 可学习、真实冻结/更新与预处理验证通过、七维库存有可行性结论。保留证据及失败；不要求证明20k收敛。

本轮机器侧证据已齐：3,000 步训练 3,000/3,000 更新完成，首 10 步 loss 均值 14.069、末 100 步均值 1.936，metrics 全部有限；500/1000/1500/2000/2500/3000 检查点完整。固定 10 任务×官方 clean 行 0—1 的 20 个开发单元全部完成且指纹唯一，10 个成功、10 个 `POLICY_DECODE_FAILURE`，没有用补零、截断或重试隐藏失败。该结果支持“链路可运行且 S 有学习信号”，但解码稳定性仍有限。

**Gate 状态：`G1=PASS`（2026-09-08）。** 负责人委托的 GPT 技术审阅检查了 `deb3bea` 的 F1 计划、执行记录、专家成功/失败回放、S 成功/失败回放及原始/重渲染对照，认为数据—动作—仿真基础链路、S 的可学习性、真实冻结/更新和预处理证据已足以通过 G1。审阅者登记为 GPT；不写成负责人本人逐帧观看，也不声称 GPT 在服务器重新执行了全部测试。

本次通过只覆盖 G1 原定范围，不代表模型已经稳定、充分收敛或可部署。10/20 开发回合成功、10/20 回合严格解码失败，全部失败保持在记录中；初态跨来源关联未知、布局同维度库存不足等限制继续保留，不声称严格初态留出，也不复制状态凑评测数量。

完整前向与 KV-cache 路径仍存在未充分归因的差异；同路径重复为零不能替代跨路径正确性证明。本事项不否决本次 G1，但列为 G2 正式协议冻结前必须核查的技术项。若后续证据确认存在位置、掩码等实现错误，须按受影响版本修复、回归并评估是否重评，旧结果和失败记录不得覆盖。

已准备好供负责人审阅的材料：一个最终 S checkpoint 的成功回合、一个解码失败回合及并排双视图视频，另有专家成功回放和 table-center/demo0 失败例的原始/重渲染对照。经本轮明确授权，少量脱敏审阅副本已发布到 [`review/`](review/)；服务器原始证据仍保留，未上传 HDF5、权重、私人规则或完整日志；审阅项目和文件名见 LOG 最新记录。

## 收尾与下一步

本次受委托技术审阅已完成 G1 所需的审阅判断，F1 无剩余机器侧工作。F1 收尾不追加训练、不重跑评测、不重复准备审阅材料，也不自动启动 F2；等待负责人给出下一阶段指令。若后续要求严格初态留出或改变库存划分，应先提交具体证据及影响评估。
